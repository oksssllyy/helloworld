#include <stdio.h>
#include <stdlib.h>
#include <board.h>

    Board print_board()
    {
        for (size_t i = 0; i < 8; i++)
        {
	    char cell = 0;
	    case 0:
	    case 7:
		cell = 'R';
		break;
	    case 1:
	    case 6:
		cell = 'N';
		break;
	    case 2:
	    case 5:
		cell = 'B';
		break;
	    case 3:
		cell = 'Q';
		break;
	    case 4:
		cell = 'K';
		break;
	    default:
		break;
	}cell.board[0][i];

        for (size_t i = 0; i < 8; i++)
        {
	    cell = 'P';
	}cell.board[1][i];

	for (size_t i = 0; i < 8; i++)
	{
	    for (size_t j = 2; j < 5; j++)
	    {
		cell = ' ';
	    }
	}cell.board[j][i];

